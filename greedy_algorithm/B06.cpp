#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    int m = 0, n = 0, res = 0;
    cin >> m;
    vector<int> q(m);
    for (int i = 0; i < m; i++){
        cin >> q[i];
    }
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++){
        cin >> a[i];
    }
    sort(q.begin(), q.end());
    sort(a.begin(), a.end());
    for (int i = 0; i < )
    return 0;
}